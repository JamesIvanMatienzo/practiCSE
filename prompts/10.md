Update DashboardScreen.kt to match the 'Progress Tracker' wireframe.  

Layout: Use a LazyColumn for the whole screen.  

Action Cards: Create two ElevatedCard components at the top: 'Start a New Exam' (with a document icon) and 'Continue Last Session' (with a play icon).  

State: Ensure these cards use Modifier.clickable and navigate to the Exam route.

Progress Section: Below the cards, add the 'Subject Performance' section. For 'Logic', 'Math', and 'Constitution', use a Row containing the subject name, a LinearProgressIndicator with a custom height, and the percentage text (e.g., 85%).  

Footer: Add the 'Ranking' and 'Study Library' buttons as OutlinedButton or TonalButton side-by-side.