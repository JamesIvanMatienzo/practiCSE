Unified Dashboard & Progress Analytics
"Act as a Senior UI Developer. Build the DashboardScreen that serves as the central hub for the user's exam preparation.  

Key Requirements:

Analytical Data Mapping:

Query the UserProgress table to calculate the percentage of correct answers vs. total attempts for each category (e.g., Logic, Math, Constitution).  

Map these percentages to the Material 3 LinearProgressIndicator bars.  

Visual Components:

Action Cards: Implement two primary cards at the top: 'Start a New Exam' and 'Continue Last Session'.  

Resume Logic: The 'Continue' button must check the UserSession table and navigate the user directly to the exact question index they last touched.  

Secondary Navigation: Include smaller buttons for 'Ranking' and 'Study Library' as seen in the wireframe.  

Connectivity State:

Include a small, non-intrusive 'Offline Mode Active' chip or indicator that changes based on the real-time network status.  

Polish:

Apply the Eye-Care color palette: Background #F8F9FA and Text #202124.  

Use the Success Green (#2E7D32) for progress bars that are above 75% and Primary Blue (#1A73E8) for everything else.  

Output: Provide the DashboardScreen Composable and the ViewModel logic required to aggregate the category-specific scores from Room."