Finalize the UI polish:

Offline Indicator: Add the 'Offline Mode Active' chip at the bottom of the screens as shown in the wireframe.  

Loading States: While the app is fetching data from Supabase for the Ranking, show a CircularProgressIndicator instead of leaving the UI unclickable.

Transitions: Ensure the HorizontalPager used for questions has a smooth 300ms transition as per our previous performance goals.