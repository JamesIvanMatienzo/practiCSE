"Act as a Senior Backend-Integration Engineer. We need to implement the Ranking feature and background synchronization for practiCSE.  Key Requirements:WorkManager Sync: Create a SyncScoresWorker that triggers only when NetworkType.CONNECTED. It should take the user's highest scores from the local UserProgress table and 'upsert' them to the cloud database (Supabase/Firebase).  Global Leaderboard: Build a RankingViewModel that fetches the top 100 global scores.Offline Handling: If the fetch fails due to no internet, the UI must display the last successfully cached ranking from Room.  UI Implementation: Build the RankingScreen based on the wireframe. It should show the user's rank clearly at the top and a list of other high-performing students below.  Security: Ensure the sync logic only sends the totalScore and userName to prevent leaking private data.  Output: Provide the SyncScoresWorker implementation and the RankingScreen Composable."  