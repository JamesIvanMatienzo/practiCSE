Jetpack Compose UI & Momentum Experience
"Act as a Senior UI/UX Engineer. Using the previously created ExamViewModel, build the ExamScreen in Jetpack Compose.
Key UI Requirements:
Layout Structure: Use a Scaffold.
Pinned Header: If Question.passage is not null, implement a static top section (outside the pager) that remains visible while the user scrolls through the question options below.
HorizontalPager: Wrap the question area in a HorizontalPager to allow for smooth swiping between the 20 or 165 items.
Momentum Feedback: When a user selects a RadioButton option:
Highlight the selection using Success Green or Error Red based on the correctness.
The UI must observe the pagerState and automatically scroll to the next page once the ViewModel's 300ms delay completes.
Error Reporting FAB: Place a Floating Action Button (FAB) labeled 'Report Error'. When clicked, it should trigger the ViewModel to flag the current questionId as inaccurate in the Room DB.
Timer Display: Add a persistent, non-intrusive timer in the top-right corner that updates based on the ViewModel's state.
Styling: Use the defined palette: Background #F8F9FA, Primary Blue #1A73E8, and Text #202124. Ensure all text is scalable using sp units."

