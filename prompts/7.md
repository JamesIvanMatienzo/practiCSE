Remote Configuration & Quality Assurance
"Act as a Senior DevOps and QA Engineer. We need to finalize practiCSE for production by implementing Remote Configuration and Crash Reporting.  

Key Requirements:

Firebase/Supabase Remote Config:

Set up a 'Passing Threshold' variable (e.g., 80%) that can be updated remotely without an app update.  

Implement a 'Feature Toggle' for the AI Step-by-Step logic so it can be disabled instantly if API costs spike or the service goes down.  

Crashlytics & Observability:

Integrate a crash reporting service (like Firebase Crashlytics).  

Ensure that any Room SQL migration errors are logged specifically so we can track data corruption issues during updates.  

Unit Testing for Accuracy:

Write a set of JUnit tests to verify the ScoreCalculator logic.  

Ensure that a 165-item mock exam correctly calculates the percentage and 'Pass/Fail' status according to official Civil Service rules.  

UI Performance Audit:

Optimize the HorizontalPager to ensure the 300ms 'Momentum' delay and animation are 'jank-free' on low-end devices.  

Output: Provide the implementation for the RemoteConfigManager and a sample ScoreCalculatorTest class."