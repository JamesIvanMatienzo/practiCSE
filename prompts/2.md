ViewModel & State Management
"Now that the Data Layer is ready, act as a Senior Android Developer to build the ExamViewModel and the UI State for the ExamScreen.
Key Requirements:
State Management: Use MutableStateFlow to expose an ExamUiState that includes the current question list, current question index, and a 'loading' or 'success' status.
Momentum Selection Logic: Implement a function selectAnswer(choiceIndex: Int).
It must immediately update the UserProgress in the Room DB.
It must implement a 300ms delay using delay() to allow the user to see if they were right/wrong (via a color change).
After the delay, it should trigger an event for the HorizontalPager to animateScrollToPage to the next index.
Resume Logic: Add a function to fetch the lastQuestionIndex from the UserSession table so the user can 'Pick up where they left off'.
Timer Logic: Implement a countdown timer for the 165-item mock exam that persists even if the app process is killed.
Output: Provide the complete ExamViewModel code and a sealed class for the ExamUiState."

